import { Request, Response, NextFunction } from 'express';
import PostModel from '../models/post.model';

export function getMyPosts(req: Request, res: Response, next: NextFunction) {
    // PostModel.find((err,posts))
}
